val data = sc.textFile("input.txt")

val words = sc.flatMap(lines => line.split("\\s+"))

val pairs = words.map(word => (word,1))

val count = pairs.reduceByKey(_ + _)

counts.collect().foreach{ case (word,count) => println("$word => $count") }
